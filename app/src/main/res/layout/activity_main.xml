<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="32dp"
    android:gravity="center"
    android:background="#FAFAFA">

    <!-- Ícone no topo da tela (usando o ic_shield que você criou) -->
    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/ic_shield"
        app:tint="#212121"
        android:layout_marginBottom="16dp"/>

    <!-- Título do App -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Site Blackhole"
        android:textSize="32sp"
        android:textStyle="bold"
        android:textColor="#212121"
        android:layout_marginBottom="8dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Bloqueie distrações definitivamente."
        android:textSize="16sp"
        android:textColor="#757575"
        android:layout_marginBottom="48dp"/>

    <!-- Campo de texto moderno -->
    <com.google.android.material.textfield.TextInputLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Ex: instagram.com"
        app:boxBackgroundMode="outline"
        app:boxCornerRadiusTopStart="12dp"
        app:boxCornerRadiusTopEnd="12dp"
        app:boxCornerRadiusBottomStart="12dp"
        app:boxCornerRadiusBottomEnd="12dp"
        android:layout_marginBottom="24dp">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/edtDomain"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textUri" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Botão de Bloquear (Vermelho/Alerta) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnAdd"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:text="Bloquear Para Sempre"
        android:textSize="16sp"
        android:textStyle="bold"
        android:backgroundTint="#D32F2F"
        app:cornerRadius="12dp"
        android:layout_marginBottom="16dp"/>

    <!-- Botão de Ativar a VPN (Azul/Seguro) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnStartVpn"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:text="Ativar Proteção"
        android:textSize="16sp"
        android:textStyle="bold"
        android:backgroundTint="#1976D2"
        app:cornerRadius="12dp" />
			<!-- Título da Lista -->
		<TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Sites no Buraco Negro:"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#212121"
        android:layout_marginTop="32dp"
        android:layout_marginBottom="8dp"/>

    <!-- A Lista em si -->
    <ListView
        android:id="@+id/listViewSites"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/transparent"
        android:dividerHeight="8dp" />

</LinearLayout>
